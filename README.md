# Free Video Gen

GeminiGen video generation client script.

## Usage

```powershell
python geminigen_video_client.py --help
```

Session files, generated videos, images, caches, and environment files are ignored by Git.
